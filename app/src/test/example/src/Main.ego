// Line comment, must be the first thing on the line (except for whitespace),
// doesn't work at the end
/**
* Doc comment
*/

/*
Block comment
*/

// optional module declaration, if there isn't any, it's part of global
// The main (entry) function is always in the global module

import {
	::loopTest
}

i32 main() {
	var boolean = true

	i8 byte = 127
	i16 short = 32767
	// i32
	var int = 10
	i64 long = 10

	u8 ubyte = 256
	u16 ushort = 65536
	u32 uint = 10
	u64 ulong = 10

	// double
	var d = 200.0
	// float
	float c = 10

	var c = 'H'
	var slStr = "${if (1 < 2) c else 'Y'}ello"
	// indentation is removed to the same depth as the start of the string
	// Hello\n$\nWorld!
	var mlStr = 
		`
		$slStr
		$$
		World!
		`

	mut #i32 arr = [1, 2, 3, 4, 5];
	// 4 byte 00000001 0111111-01111111 11111111
	//var bitMap = {{ ..., 1::1 , byte, short }};
	var touple = {"asd", 'c', 10, 10.0};

	ret 0
}
